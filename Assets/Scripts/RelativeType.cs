namespace ContextIII
{
    public enum RelativeType
    {
        Stationary = 0,
        Moving_SendToServer = 1,   // Will be send to Server
        Moving_ReceiveFromServer = 2,  // Will be receive from server
    }
}